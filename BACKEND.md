# Backend / Admin setup

این پروژه اکنون شامل فرم مودال، API سمت سرور و پنل مدیریت پذیرندگان است.

## 1) اتصال ذخیره‌سازی

برای محیط Vercel یک Redis/Upstash KV متصل کنید و متغیرهای زیر را در Project Settings → Environment Variables قرار دهید:

- `KV_REST_API_URL`
- `KV_REST_API_TOKEN`
- `ADMIN_TOKEN`

`ADMIN_TOKEN` باید یک مقدار تصادفی و طولانی باشد؛ در کد فرانت‌اند یا Git commit نشود.

## 2) پنل مدیریت

بعد از دیپلوی، مسیر `/admin/` را باز کنید و با `ADMIN_TOKEN` وارد شوید.

امکانات: لیست درخواست‌ها، جستجو، فیلتر وضعیت، مشاهده جزئیات، تغییر وضعیت، ثبت یادداشت و خروجی CSV.

## 3) اعتبارسنجی

سمت کلاینت و سرور هر دو اعتبارسنجی انجام می‌دهند. موارد فعلی شامل کد ملی با check digit، شماره کارت با Luhn، شبا با MOD-97، موبایل و تلفن ایران، کد پستی ۱۰ رقمی، تاریخ شمسی، فیلدهای اجباری، شرطی‌بودن اجاره‌نامه و استشهاد محلی و تطبیق شهر با استان است.

## 4) داده‌های اینترنتی

استان/شهر از `Iran Places API` خوانده می‌شود و در هنگام ثبت، جفت استان/شهر دوباره در سمت سرور بررسی می‌شود.

برای اعتبارسنجی واقعی‌تر کد پستی، در صورت تهیه API key، متغیر `POSTALDATA_API_KEY` را تنظیم کنید. برای سرویس استعلام کد ملی نیز `NATIONAL_ID_API_URL` و `NATIONAL_ID_API_KEY` قابل تنظیم هستند. بدون کلید، اعتبارسنجی محلی و ساختاری همچنان فعال است؛ بنابراین این سیستم ادعای استعلام ثبت‌احوال یا پست را ندارد.

## 5) فایل مرجع فرم

ساختار فرم وب با فرم «مشخصات پذیرندگان» موجود در فایل مرجع تطبیق داده شده و مواردی که در نسخه وب شرطی هستند با منطق کسب‌وکار نمایش داده می‌شوند. «استشهاد محلی» در این پروژه به‌صورت ساختار اطلاعاتی پیاده شده و عنوان فرم رسمی بانک ادعا نمی‌شود.
